# Paquetes

Paquetes previstos: `instituciones`, `administracion`, `academico` y `estudiantes` en el backend; páginas equivalentes, servicios de API y esquemas de formulario en el frontend. Las dependencias comunes de permisos e institución se reutilizarán desde el núcleo de acceso.
